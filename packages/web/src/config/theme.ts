/**
 * The palette lives in `@soromi/ui/theme.css` as CSS variables (`--soromi-*`). This holds the few
 * color values JS itself needs (the xterm terminal theme).
 */
export const colors = {
  bgTerminal: '#0a0a0b',
  text: '#f0f0f0',
}
